# Known Technical Debt & Issues

This document tracks active technical debt and known system limitations.

- **Graphify Open AI Backend Dependency**: Graphify requires `graphifyy[gemini]` or `openai` package for full semantic extraction. Resolved by auto-installing full backends in setup.
